# Laporan Jobsheet 6

1. Menambahkan fitur **AJAX** pada halaman list buku dan anggota dengan menggunakan file json sebagai source data.

2. Menambahkan loading indicator dan simulasi loading menggunakan `Promise` + `setTimeOut`.

3. Menambahkan penanganan eror menggunakan `try/catch`.

### Modifikasi

1. Menambahkan fungsi generic `fetchData(url)` untuk kedua halaman (list anggota & list buku).

2. Menambahkan kolom baru di list buku (kategori) dan list anggota (tanggal bergabung).

3. Mengganti delay simulator menjadi 3 detik melalui parameter `delaySimulation`.

4. (Tambahan Pribadi) Modifikasi style `loading-indicator`.