# Laporan Jobsheet 7

1. Mengubah semua page `html` menjadi `php`.

2. Memisah `<header>` dan `<footer>` menjadi komponen terpisah.

3. Menerapkan session sebagai penyimpanan data dan fitur alert.

# Modifikasi

1. Menambahkan `proses_tambah.php` untuk anggota.

2. Menambahkan komponen alert di halaman form `tambah.php` (buku & anggota).

3. Membuat beberapa analitik di `index.php` menjadi dinamis.

4. Tweak `footer.php` style.

5. Tweak style `tambah.php` (buku & anggota). 

6. Memindahkan link tambah buku dan anggota pada halaman `list.php` sebagai tombol link.