# Laporan Jobsheet 2

1. Menambahkan `assets/css/style.css` beserta _link stylesheet_ di masing-masing halaman.

2. Memberikan style kepada semua komponen, mulai dari: css reset, body, header, main & section, table, form, dan footer.

### Modifikasi

1. Memperbaiki style `h2` pada section main di `index.html`.

2. Membuat grid menjadi lebih responsif dengan `auto-fit` dan `minmax`.

3. Menambahkan tombol ketiga di tabel buku.