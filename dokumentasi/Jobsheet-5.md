# Laporan Jobsheet 5

1. Menambahkan fitur interaktifitas menggunakan javascript (app.js), diantaranya: tweak hamburger menu, fitur search, validasi form, dan konfirmasi hapus.

2. Melakukan penyesuaian pada elemen html, seperti: menambahkan tag `<script>` di setiap halaman, mengubah hamburger menu menjadi `<button>`, menambahkan class `btn-hapus` pada tombol hapus di table, menambahkan id `form-tambah` pada tag form, dsb.

### Modifikasi

1. Menambahkan validasi field ISBN menggunakan *regex*.
2. Menambahkan sedikit animasi pada hamburger menu di tampilan mobile.
3. Modifikasi fitur search dimana pencarian dibataskan pada kolom `Judul` saja.